name: PowerShell

on:
  push:
  workflow_dispatch:

jobs:
  build-dependencies:
    name: Build Dependencies
    runs-on: windows-latest
    steps:
      - name: Install Salesforce
        run: sh "npm install --global @salesforce/cli@2.5.8"

